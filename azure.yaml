name: flappy-vs
infra:
  provider: bicep
services:
  web:
    project: flappy-vs
    host: staticwebapp
    language: ts
  # Ensure azd deploy includes the built app and the managed Functions API
  dist: dist
  api: api
